import React from "react";

interface IMenuButtonProps {
  imgSrc: string;
  title: string;
}

const MenuButton: React.FC<IMenuButtonProps> = ({ imgSrc, title }) => {
  return <div></div>;
};

export default MenuButton;
